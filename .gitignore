node_modules/
dist/
data/*.db
data/*.db-shm
data/*.db-wal
.env
.env.local

# User-uploaded media (lives on the server's persistent disk, never in Git)
data/uploads/

# Database dumps and backups (may contain credentials — never commit)
sutra-dump.sql
sutra-dump-slim.sql
sutra-backup-*.sql
restore-content.sql
turso-import.sql
turso-upload.db
*.dump.sql
