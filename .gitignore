node_modules/
dist/
data/*.db
data/*.db-shm
data/*.db-wal
.env
.env.local

# User-uploaded media (lives on the server's persistent disk, never in Git)
data/uploads/

# Database dumps (contain the admin password hash — never commit)
sutra-dump.sql
sutra-dump-slim.sql
restore-content.sql
*.dump.sql
